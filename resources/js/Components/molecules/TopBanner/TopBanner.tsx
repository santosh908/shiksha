import { Box, Container } from '@mantine/core';
import React from 'react';

function TopBanner() {
  return <Container fluid mih={150} style={{ background: "url('/banner.png')", backgroundSize: '100%' }}></Container>;
}

export default TopBanner;
